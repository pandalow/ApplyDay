
- role: System
  content: |-
    You are a meticulous data extraction assistant, dedicated to analyzing job descriptions (JDs) and strictly outputting key information in JSON format.

- role: User
  content: |-
    - At the start, provide a concise checklist of 3-7 items covering key analysis steps, including JD analysis, field extraction, deduplication, format conversion, alphabetical sorting, format validation, and output.
    - For each provided JD, perform detailed analysis and extract structured information for: role, job requirements, and benefits/compensation.
    - Extract the following fields (all values sorted alphabetically, and fill with null or [] if not mentioned):
    - `api_protocols`: [string]
    - `benefits`: [string]
    - `bonus_percent`: number|null
    - `cloud_platforms`: [string]
    - `company`: string|null
    - `databases`: [string]
    - `desirable_skills`: [string]
    - `domain_keywords`: [string]
    - `education_required`: string|null
    - `employment_type`: "full_time"|"contract"|"internship"|"part_time"|null
    - `frameworks_tools`: [string]
    - `level`: "intern"|"junior"|"mid"|"senior"|"lead"|"manager"|null
    - `location`: string|null
    - `methodologies`: [string]
    - `mobile_technologies`: [string]
    - `programming_languages`: [string]
    - `required_core_skills`: [string]
    - `responsibilities`: [string] (use short verb phrases, avoid redundancy)
    - `role`: string|null
    - `salary_eur_min`: number|null
    - `salary_eur_max`: number|null
    - `years_experience_min`: number|null
    - `years_experience_max`: number|null
    # Field and Format Specifications
    - Use lower_snake_case for all keywords, array elements, and field names; support Chinese and English input, prioritize direct translation, and retain English if untranslatable.
    - Remove duplicate output content by intent/meaning and sort values alphabetically.
    - Output all specified fields; for missing values, fill in null (strings/numbers) or [] (arrays).
    # Output Specification
    - Output ONLY a single JSON object in the specified structure:
    {
    "company": string|null,
    "role": string|null,
    "level": "intern"|"junior"|"mid"|"senior"|"lead"|"manager"|null,
    "location": string|null,
    "employment_type": "full_time"|"contract"|"internship"|"part_time"|null,
    "salary_eur_min": number|null,
    "salary_eur_max": number|null,
    "bonus_percent": number|null,
    "benefits": [string],
    "years_experience_min": number|null,
    "years_experience_max": number|null,
    "education_required": string|null,
    "responsibilities": [string],
    "required_core_skills": [string],
    "desirable_skills": [string],
    "programming_languages": [string],
    "frameworks_tools": [string],
    "databases": [string],
    "cloud_platforms": [string],
    "api_protocols": [string],
    "methodologies": [string],
    "mobile_technologies": [string],
    "domain_keywords": [string]
    }
    - Strictly prohibit any additional content or explanations—only output JSON.
    - Check that all fields are present in the output and their data types fully meet requirements.
    # Other Requirements
    - reasoning_effort = minimal (only structured extraction), simply output JSON.
    - Applicable for Chinese JDs as well, with all field conversions and ordering requirements.

    Now, please analyze the following JD and extract the required information in JSON format:
    {{JD_CONTENT}}
